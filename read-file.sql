declare
f1 UTL_FILE.FILE_TYPE;
ct binary_integer;
BRCH VARCHAR2(5);
BEGIN
f1 := UTL_FILE.FOPEN('DESKTOP','sql1.txt','R');


LOOP

BEGIN
UTL_FILE.GET_LINE(f1,BRCH);
DBMS_OUTPUT.PUT_LINE(BRCH);
INSERT INTO brch values(BRCH);
EXCEPTION
WHEN NO_DATA_FOUND THEN
EXIT;
END;

END LOOP;

UTL_FILE.FCLOSE (f1);
END;
/